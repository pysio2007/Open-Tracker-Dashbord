name = "open-tracker-dashboard-frontend"
compatibility_date = "2024-01-01"

# 静态资源配置
[site]
bucket = "."

# 部署配置
[env.production]
workers_dev = true
route = { pattern = "dashboard.your-domain.com/*", custom_domain = true }  # 需要替换为实际域名 